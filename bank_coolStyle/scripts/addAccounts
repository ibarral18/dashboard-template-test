function account(firstName, lastName) = {
  return{
    firstName,
    lastName,
    balance: 0,
    accountNumber: Math.floor(100000000 + Math.random() * 900000000),
  }
};

function addition(){
  
}
